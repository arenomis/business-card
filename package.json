{
  "name": "business-card",
  "version": "1.0.0",
  "private": true,
  "description": "Лендинг-визитка: Angular + Node API (форма, почта, ассистент по резюме)",
  "scripts": {
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "dev": "concurrently -n api,web -c blue,green \"npm run dev --prefix backend\" \"npm start --prefix frontend\"",
    "dev:api": "npm run dev --prefix backend",
    "dev:web": "npm start --prefix frontend",
    "build": "npm run build --prefix frontend",
    "start:api": "npm start --prefix backend",
    "test:api": "npm test --prefix backend"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
